
class QuickSort {
    constructor() {

    }

    sort() {

    }

    partition(arr) {
        const p = arr[0];
        for(let i =0; i< arr.length; i++) {
            
        }
    }

    swap(arr, i, j) {
        const temp = arr[i];
        arr[i] = arr[j];
        arr[j] = temp;
    }
}
